'use strict';

describe('Service: restAPI', function() {

  // load the service's module
  // beforeEach(module('flickrportfolioApp'));

  // instantiate service
  // var restAPI;

  // beforeAll(inject(function(_restAPI_) {
  //   restAPI = _restAPI_;
  // }));

  it('get stuff from Flickr', function() {
    expect(true).toBe(true);
  });
});
